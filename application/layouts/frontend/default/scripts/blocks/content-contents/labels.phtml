<?php
/* AVAILABLE OPTIONS:
 *
 * types
 * limit
 * form_filter
 * search_result
 * template
 */

if($this->contents): ?>

    <div class="block block-content-contents-labels <?php echo $this->cssVisibility($this->block); ?>">
        <?php if($this->block->getOption('form_filter')): ?>
        <fieldset>
            <legend>Busca avançada</legend>
            <?php echo $this->formFilter; ?>
        </fieldset>
        <?php endif; ?>

        <ul class="inline">
            <?php foreach($this->contents as $content): ?>
            <li>
                <a href="<?php echo $this->baseUrl($content->slug); ?>">
                    <div class="title label label-info">
                        <?php echo $this->escape($content->title); ?>
                    </div>
                </a>
            </li>
            <?php endforeach; ?>
        </ul>
    </div>

<?php elseif($this->block->getOption('search_result')): ?>

    <p class="info">Nenhum resultado encontrado.</p>

<?php endif; ?>